# Frontend production environment variables
# Replace values and copy to .env.production before building/deploying

# Railway backend public URL (no trailing slash)
VITE_BACKEND_URL=https://your-service.up.railway.app

# Base path when hosting under a subpath (GitHub Pages repo)
# e.g., if site is served at https://username.github.io/repo-name/ then set:
# VITE_BASE_PATH=/repo-name/
VITE_BASE_PATH=/